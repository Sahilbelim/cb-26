#include <stdio.h>
void main()
{
    int numbers[10],k;

    numbers[0] = 10;
    numbers[1] = 20;
    numbers[2] = 30;
    numbers[3] = 40;
    numbers[4] = 30;
    numbers[5] = 60;
    numbers[6] = 80;
    numbers[7] = 80;
    numbers[8] = 100;
    numbers[9] = 110;

    for (k = 0; k <= 9;k++)
    {
        printf("%d ", numbers[k]);
   }
    // printf("%d ", numbers[k]);
    // k++;
    // printf("%d ", numbers[k]);
    // k++;
    // printf("%d ", numbers[k]);
    // k++;
    // printf("%d ", numbers[k]);
    // k++;
    // printf("%d ", numbers[k]);
    // k++;
    // printf("%d ", numbers[k]);
    // k++;
    // printf("%d ", numbers[k]);
    // k++;
    // printf("%d ", numbers[k]);
    // k++;
    // printf("%d ", numbers[k]);
    // k++;
}