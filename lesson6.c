#include <stdio.h>
void main()
{
    int table_number, i, mul;

    table_number = 75;

    i = 1;

    mul = table_number * 1;

    printf(" %d x %d = %d \n", table_number, i, mul);

    i = 2;

    mul = table_number * i;

    printf(" %d x %d = %d \n", table_number, i, mul);

    i = 3;

    mul = table_number * i;

    printf(" %d x %d = %d \n", table_number, i, mul);

    i = 4;

    mul = table_number * i;

    printf(" %d x %d = %d \n", table_number, i, mul);

    i = 5;

    mul = table_number * i;

    printf(" %d x %d = %d \n", table_number, i, mul);

    i = 6;

    mul = table_number * i;

    printf(" %d x %d = %d \n", table_number, i, mul);

    i = 7;

    mul = table_number * i;

    printf(" %d x %d = %d \n", table_number, i, mul);

    i = 8;

    mul = table_number * i;

    printf(" %d x %d = %d \n", table_number, i, mul);

    i = 9;

    mul = table_number * i;

    printf(" %d x %d = %d \n", table_number, i, mul);

    i = 10;

    mul = table_number * i;

    printf(" %d x %d = %d \n", table_number, i, mul);

}